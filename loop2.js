//Avec une boucle do-while afficher les éléments du tableau suivant, ligne par ligne:

let tab = ['a', 'b', 'c', 'd', 'e']
console.log(tab) // to be changed

let i = 0 // index 0

do {
  console.log(tab[i]) 
  i++  // increment assign
} while (i < tab.length) // until length